var euSouUmaVariavel  = 3
let euSouUmaVariavelModerna = 4

// Ok, "var" é uma maneira mais antiga de se declarar variaveis, enquanto "let" é mais moderno. Mas, no fundo
// eles tem o mesmo fim: serem variaveis. Ou seja, você pode atribuir novos valores a eles.
// Contudo, uma coisa importante: o "var" pode ser declarado e atribuido mais de uma vez. Já o "let", não;  

const euSouUmaConstante = 3

// Constante geralment é o melhor caminho, o caminho "primario". Ou seja, se uma variável não precisa ser
// altrada em momento algum, então use-a como "const". E nem sempre é bom, em uma programação funcional,
// é um pouco confuso constantemente alterar dados.